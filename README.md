# Zepto_Dataset_Analysis
This project focuses on cleaning, processing, and analyzing a real-world Zepto product dataset using MySQL. The dataset contains several data quality issues such as NULL values, duplicate records, empty cells, inconsistent data formats, and incorrect entries, which commonly occur in practical database environments.
